name(x , y) {
	( x + y + ("foo" + y) + ((foo("foo"))) ) + x
}

name2(x, y) {
	if (if(x = y) x else d in ("fooo"))
		( x_2 + y + ("foo" + y) + ((foo())) ) + x
	else
		if (y = "foo")
			( x + y + ("foo" + y) + ((foo())) ) + x
		else
			"foo" + x + foo("FOO") + x + (x)
}
